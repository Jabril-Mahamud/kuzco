# AI CLI Configuration Example
# Copy this file to .env and modify as needed

# Default model to use (leave empty for interactive selection)
DEFAULT_MODEL=

# Safe mode settings
SAFE_MODE=true
CREATE_BACKUPS=true

# Display settings
MAX_PREVIEW_SIZE=2000
DEFAULT_THEME=monokai

# Command execution settings
COMMAND_TIMEOUT=30

# Animation settings
SPINNER_STYLE=dots
ANIMATION_SPEED=0.1

# Exit commands (comma-separated)
EXIT_COMMANDS=exit,quit,bye,goodbye

# Sudo prefixes that require warnings (comma-separated)
SUDO_PREFIXES=sudo,su
